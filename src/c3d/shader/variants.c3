// Generated by build_shaders.py from shaders/variants.json - do not edit.
module c3d::shader;

<*
 Stage families compiled into the registry.
*>
enum ShaderName : int {
    FULLSCREEN,
    GRADIENT,
    COMPOSITE,
    MESH,
    BASIC,
    IMGUI_VERTEX,
    IMGUI_FRAGMENT,
    CUBE_PREVIEW,
    STANDARD,
    PHYSICAL,
    TOON,
    ENVIRONMENT_SOURCE,
    ENVIRONMENT_PREFILTER,
    SHEEN_PREFILTER,
    IRRADIANCE_PROJECT,
    IRRADIANCE_REDUCE,
    BRDF_LUT,
    SHEEN_LUT,
    SKY,
    DEPTH_ONLY,
    GRADE,
    FXAA,
    DISPLAY,
    BLOOM_DOWNSAMPLE,
    BLOOM_UPSAMPLE,
    TILE_MAX,
    TILE_NEIGHBOR,
    DOF_COC,
    DOF_GATHER,
    DOF_COMPOSITE,
}

<*
 Flag bit for DEPTH_ONLY.
*>
const uint SHADER_FLAG_DEPTH_ONLY = 1 << 0;

<*
 Flag bit for SKINNED.
*>
const uint SHADER_FLAG_SKINNED = 1 << 1;

<*
 Flag bit for SKINNED_U16.
*>
const uint SHADER_FLAG_SKINNED_U16 = 1 << 2;

<*
 Flag bit for MORPH.
*>
const uint SHADER_FLAG_MORPH = 1 << 3;

const char[*] FULLSCREEN_SPIRV @private = $embed("../../../shaders/spv/fullscreen.spv");
const char[*] GRADIENT_SPIRV @private = $embed("../../../shaders/spv/gradient.spv");
const char[*] COMPOSITE_SPIRV @private = $embed("../../../shaders/spv/composite.spv");
const char[*] MESH_SPIRV @private = $embed("../../../shaders/spv/mesh.spv");
const char[*] BASIC_SPIRV @private = $embed("../../../shaders/spv/basic.spv");
const char[*] IMGUI_VERTEX_SPIRV @private = $embed("../../../shaders/spv/imgui_vertex.spv");
const char[*] IMGUI_FRAGMENT_SPIRV @private = $embed("../../../shaders/spv/imgui_fragment.spv");
const char[*] CUBE_PREVIEW_SPIRV @private = $embed("../../../shaders/spv/cube_preview.spv");
const char[*] STANDARD_SPIRV @private = $embed("../../../shaders/spv/standard.spv");
const char[*] PHYSICAL_SPIRV @private = $embed("../../../shaders/spv/physical.spv");
const char[*] TOON_SPIRV @private = $embed("../../../shaders/spv/toon.spv");
const char[*] ENVIRONMENT_SOURCE_SPIRV @private = $embed("../../../shaders/spv/environment_source.spv");
const char[*] ENVIRONMENT_PREFILTER_SPIRV @private = $embed("../../../shaders/spv/environment_prefilter.spv");
const char[*] SHEEN_PREFILTER_SPIRV @private = $embed("../../../shaders/spv/sheen_prefilter.spv");
const char[*] IRRADIANCE_PROJECT_SPIRV @private = $embed("../../../shaders/spv/irradiance_project.spv");
const char[*] IRRADIANCE_REDUCE_SPIRV @private = $embed("../../../shaders/spv/irradiance_reduce.spv");
const char[*] BRDF_LUT_SPIRV @private = $embed("../../../shaders/spv/brdf_lut.spv");
const char[*] SHEEN_LUT_SPIRV @private = $embed("../../../shaders/spv/sheen_lut.spv");
const char[*] SKY_SPIRV @private = $embed("../../../shaders/spv/sky.spv");
const char[*] MESH_DEPTH_ONLY_SPIRV @private = $embed("../../../shaders/spv/mesh_depth_only.spv");
const char[*] DEPTH_ONLY_SPIRV @private = $embed("../../../shaders/spv/depth_only.spv");
const char[*] MESH_SKINNED_SPIRV @private = $embed("../../../shaders/spv/mesh_skinned.spv");
const char[*] MESH_SKINNED_U16_SPIRV @private = $embed("../../../shaders/spv/mesh_skinned_u16.spv");
const char[*] MESH_MORPH_SPIRV @private = $embed("../../../shaders/spv/mesh_morph.spv");
const char[*] MESH_SKINNED_MORPH_SPIRV @private = $embed("../../../shaders/spv/mesh_skinned_morph.spv");
const char[*] MESH_SKINNED_U16_MORPH_SPIRV @private = $embed("../../../shaders/spv/mesh_skinned_u16_morph.spv");
const char[*] MESH_DEPTH_ONLY_SKINNED_SPIRV @private = $embed("../../../shaders/spv/mesh_depth_only_skinned.spv");
const char[*] MESH_DEPTH_ONLY_SKINNED_U16_SPIRV @private = $embed("../../../shaders/spv/mesh_depth_only_skinned_u16.spv");
const char[*] MESH_DEPTH_ONLY_MORPH_SPIRV @private = $embed("../../../shaders/spv/mesh_depth_only_morph.spv");
const char[*] MESH_DEPTH_ONLY_SKINNED_MORPH_SPIRV @private = $embed("../../../shaders/spv/mesh_depth_only_skinned_morph.spv");
const char[*] MESH_DEPTH_ONLY_SKINNED_U16_MORPH_SPIRV @private = $embed("../../../shaders/spv/mesh_depth_only_skinned_u16_morph.spv");
const char[*] GRADE_SPIRV @private = $embed("../../../shaders/spv/grade.spv");
const char[*] FXAA_SPIRV @private = $embed("../../../shaders/spv/fxaa.spv");
const char[*] DISPLAY_SPIRV @private = $embed("../../../shaders/spv/display.spv");
const char[*] BLOOM_DOWNSAMPLE_SPIRV @private = $embed("../../../shaders/spv/bloom_downsample.spv");
const char[*] BLOOM_UPSAMPLE_SPIRV @private = $embed("../../../shaders/spv/bloom_upsample.spv");
const char[*] TILE_MAX_SPIRV @private = $embed("../../../shaders/spv/tile_max.spv");
const char[*] TILE_NEIGHBOR_SPIRV @private = $embed("../../../shaders/spv/tile_neighbor.spv");
const char[*] DOF_COC_SPIRV @private = $embed("../../../shaders/spv/dof_coc.spv");
const char[*] DOF_GATHER_SPIRV @private = $embed("../../../shaders/spv/dof_gather.spv");
const char[*] DOF_COMPOSITE_SPIRV @private = $embed("../../../shaders/spv/dof_composite.spv");

<*
 Every compiled stage, in manifest order.
*>
const ShaderEntry[41] SHADER_ENTRIES = {
    { .variant = { .shader = FULLSCREEN }, .spirv = FULLSCREEN_SPIRV[..] },
    { .variant = { .shader = GRADIENT }, .spirv = GRADIENT_SPIRV[..] },
    { .variant = { .shader = COMPOSITE }, .spirv = COMPOSITE_SPIRV[..] },
    { .variant = { .shader = MESH }, .spirv = MESH_SPIRV[..] },
    { .variant = { .shader = BASIC }, .spirv = BASIC_SPIRV[..] },
    { .variant = { .shader = IMGUI_VERTEX }, .spirv = IMGUI_VERTEX_SPIRV[..] },
    { .variant = { .shader = IMGUI_FRAGMENT }, .spirv = IMGUI_FRAGMENT_SPIRV[..] },
    { .variant = { .shader = CUBE_PREVIEW }, .spirv = CUBE_PREVIEW_SPIRV[..] },
    { .variant = { .shader = STANDARD }, .spirv = STANDARD_SPIRV[..] },
    { .variant = { .shader = PHYSICAL }, .spirv = PHYSICAL_SPIRV[..] },
    { .variant = { .shader = TOON }, .spirv = TOON_SPIRV[..] },
    { .variant = { .shader = ENVIRONMENT_SOURCE }, .spirv = ENVIRONMENT_SOURCE_SPIRV[..] },
    { .variant = { .shader = ENVIRONMENT_PREFILTER }, .spirv = ENVIRONMENT_PREFILTER_SPIRV[..] },
    { .variant = { .shader = SHEEN_PREFILTER }, .spirv = SHEEN_PREFILTER_SPIRV[..] },
    { .variant = { .shader = IRRADIANCE_PROJECT }, .spirv = IRRADIANCE_PROJECT_SPIRV[..] },
    { .variant = { .shader = IRRADIANCE_REDUCE }, .spirv = IRRADIANCE_REDUCE_SPIRV[..] },
    { .variant = { .shader = BRDF_LUT }, .spirv = BRDF_LUT_SPIRV[..] },
    { .variant = { .shader = SHEEN_LUT }, .spirv = SHEEN_LUT_SPIRV[..] },
    { .variant = { .shader = SKY }, .spirv = SKY_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_DEPTH_ONLY }, .spirv = MESH_DEPTH_ONLY_SPIRV[..] },
    { .variant = { .shader = DEPTH_ONLY }, .spirv = DEPTH_ONLY_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_SKINNED }, .spirv = MESH_SKINNED_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_SKINNED_U16 }, .spirv = MESH_SKINNED_U16_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_MORPH }, .spirv = MESH_MORPH_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_SKINNED | SHADER_FLAG_MORPH }, .spirv = MESH_SKINNED_MORPH_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_SKINNED_U16 | SHADER_FLAG_MORPH }, .spirv = MESH_SKINNED_U16_MORPH_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_DEPTH_ONLY | SHADER_FLAG_SKINNED }, .spirv = MESH_DEPTH_ONLY_SKINNED_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_DEPTH_ONLY | SHADER_FLAG_SKINNED_U16 }, .spirv = MESH_DEPTH_ONLY_SKINNED_U16_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_DEPTH_ONLY | SHADER_FLAG_MORPH }, .spirv = MESH_DEPTH_ONLY_MORPH_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_DEPTH_ONLY | SHADER_FLAG_SKINNED | SHADER_FLAG_MORPH }, .spirv = MESH_DEPTH_ONLY_SKINNED_MORPH_SPIRV[..] },
    { .variant = { .shader = MESH, .flags = SHADER_FLAG_DEPTH_ONLY | SHADER_FLAG_SKINNED_U16 | SHADER_FLAG_MORPH }, .spirv = MESH_DEPTH_ONLY_SKINNED_U16_MORPH_SPIRV[..] },
    { .variant = { .shader = GRADE }, .spirv = GRADE_SPIRV[..] },
    { .variant = { .shader = FXAA }, .spirv = FXAA_SPIRV[..] },
    { .variant = { .shader = DISPLAY }, .spirv = DISPLAY_SPIRV[..] },
    { .variant = { .shader = BLOOM_DOWNSAMPLE }, .spirv = BLOOM_DOWNSAMPLE_SPIRV[..] },
    { .variant = { .shader = BLOOM_UPSAMPLE }, .spirv = BLOOM_UPSAMPLE_SPIRV[..] },
    { .variant = { .shader = TILE_MAX }, .spirv = TILE_MAX_SPIRV[..] },
    { .variant = { .shader = TILE_NEIGHBOR }, .spirv = TILE_NEIGHBOR_SPIRV[..] },
    { .variant = { .shader = DOF_COC }, .spirv = DOF_COC_SPIRV[..] },
    { .variant = { .shader = DOF_GATHER }, .spirv = DOF_GATHER_SPIRV[..] },
    { .variant = { .shader = DOF_COMPOSITE }, .spirv = DOF_COMPOSITE_SPIRV[..] },
};
