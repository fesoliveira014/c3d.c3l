// Generated by gen_shader_abi from the 'c3d' ABI schema - do not edit.
module c3d::shader;

import gpu;

struct CompositeRoot {
    TextureIndex source_texture;
    SamplerIndex source_sampler;
    uint         _pad0;
    uint         _pad1;
}
$assert CompositeRoot::size == 16;
$assert $reflect(CompositeRoot.source_texture).offset == 0;
$assert $reflect(CompositeRoot.source_sampler).offset == 4;
$assert $reflect(CompositeRoot._pad0).offset == 8;
$assert $reflect(CompositeRoot._pad1).offset == 12;

struct GeometryRoot {
    GpuAddress positions;
    GpuAddress normals;
    GpuAddress tangents;
    GpuAddress uv0;
    GpuAddress uv1;
    GpuAddress colors;
    GpuAddress joints;
    GpuAddress weights;
    GpuAddress morph_deltas;
    GpuAddress custom_data;
    uint       custom_stride;
    uint       vertex_count;
    uint       morph_target_count;
    uint       flags;
    uint       _pad0;
    uint       _pad1;
}
$assert GeometryRoot::size == 104;
$assert $reflect(GeometryRoot.positions).offset == 0;
$assert $reflect(GeometryRoot.normals).offset == 8;
$assert $reflect(GeometryRoot.tangents).offset == 16;
$assert $reflect(GeometryRoot.uv0).offset == 24;
$assert $reflect(GeometryRoot.uv1).offset == 32;
$assert $reflect(GeometryRoot.colors).offset == 40;
$assert $reflect(GeometryRoot.joints).offset == 48;
$assert $reflect(GeometryRoot.weights).offset == 56;
$assert $reflect(GeometryRoot.morph_deltas).offset == 64;
$assert $reflect(GeometryRoot.custom_data).offset == 72;
$assert $reflect(GeometryRoot.custom_stride).offset == 80;
$assert $reflect(GeometryRoot.vertex_count).offset == 84;
$assert $reflect(GeometryRoot.morph_target_count).offset == 88;
$assert $reflect(GeometryRoot.flags).offset == 92;
$assert $reflect(GeometryRoot._pad0).offset == 96;
$assert $reflect(GeometryRoot._pad1).offset == 100;

struct FrameRoot {
    Mat4f      view;
    Mat4f      proj;
    Mat4f      view_proj;
    Mat4f      inv_view_proj;
    Mat4f      prev_view_proj;
    Vec4f      camera_position;
    Vec4f      camera_params;
    Vec4f      jitter_time;
    GpuAddress lights;
    GpuAddress shadows;
    GpuAddress environment;
    GpuAddress instances;
    uint       light_count;
    uint       shadow_count;
    uint       flags;
    uint       frame_index;
    Vec4f      ambient;
}
$assert FrameRoot::size == 432;
$assert $reflect(FrameRoot.view).offset == 0;
$assert $reflect(FrameRoot.proj).offset == 64;
$assert $reflect(FrameRoot.view_proj).offset == 128;
$assert $reflect(FrameRoot.inv_view_proj).offset == 192;
$assert $reflect(FrameRoot.prev_view_proj).offset == 256;
$assert $reflect(FrameRoot.camera_position).offset == 320;
$assert $reflect(FrameRoot.camera_params).offset == 336;
$assert $reflect(FrameRoot.jitter_time).offset == 352;
$assert $reflect(FrameRoot.lights).offset == 368;
$assert $reflect(FrameRoot.shadows).offset == 376;
$assert $reflect(FrameRoot.environment).offset == 384;
$assert $reflect(FrameRoot.instances).offset == 392;
$assert $reflect(FrameRoot.light_count).offset == 400;
$assert $reflect(FrameRoot.shadow_count).offset == 404;
$assert $reflect(FrameRoot.flags).offset == 408;
$assert $reflect(FrameRoot.frame_index).offset == 412;
$assert $reflect(FrameRoot.ambient).offset == 416;

struct DrawRoot {
    Mat4f      model;
    Mat4f      prev_model;
    Vec4f      normal_0;
    Vec4f      normal_1;
    Vec4f      normal_2;
    GpuAddress frame;
    GpuAddress geometry;
    GpuAddress material;
    GpuAddress skin;
    GpuAddress morph;
    GpuAddress instance_data;
    GpuAddress previous_pose;
    uint       object_id;
    uint       flags;
    uint       layers;
    uint       _pad1;
    uint       _pad2;
    uint       _pad3;
}
$assert DrawRoot::size == 256;
$assert $reflect(DrawRoot.model).offset == 0;
$assert $reflect(DrawRoot.prev_model).offset == 64;
$assert $reflect(DrawRoot.normal_0).offset == 128;
$assert $reflect(DrawRoot.normal_1).offset == 144;
$assert $reflect(DrawRoot.normal_2).offset == 160;
$assert $reflect(DrawRoot.frame).offset == 176;
$assert $reflect(DrawRoot.geometry).offset == 184;
$assert $reflect(DrawRoot.material).offset == 192;
$assert $reflect(DrawRoot.skin).offset == 200;
$assert $reflect(DrawRoot.morph).offset == 208;
$assert $reflect(DrawRoot.instance_data).offset == 216;
$assert $reflect(DrawRoot.previous_pose).offset == 224;
$assert $reflect(DrawRoot.object_id).offset == 232;
$assert $reflect(DrawRoot.flags).offset == 236;
$assert $reflect(DrawRoot.layers).offset == 240;
$assert $reflect(DrawRoot._pad1).offset == 244;
$assert $reflect(DrawRoot._pad2).offset == 248;
$assert $reflect(DrawRoot._pad3).offset == 252;

struct BasicMaterialGpu {
    uint         kind;
    uint         flags;
    float        alpha_cutoff;
    uint         _pad0;
    Vec4f        color;
    TextureIndex map_texture;
    SamplerIndex map_sampler;
    uint         map_uv_set;
    uint         map_present;
    Vec4f        map_uv_row0;
    Vec4f        map_uv_row1;
}
$assert BasicMaterialGpu::size == 80;
$assert $reflect(BasicMaterialGpu.kind).offset == 0;
$assert $reflect(BasicMaterialGpu.flags).offset == 4;
$assert $reflect(BasicMaterialGpu.alpha_cutoff).offset == 8;
$assert $reflect(BasicMaterialGpu._pad0).offset == 12;
$assert $reflect(BasicMaterialGpu.color).offset == 16;
$assert $reflect(BasicMaterialGpu.map_texture).offset == 32;
$assert $reflect(BasicMaterialGpu.map_sampler).offset == 36;
$assert $reflect(BasicMaterialGpu.map_uv_set).offset == 40;
$assert $reflect(BasicMaterialGpu.map_present).offset == 44;
$assert $reflect(BasicMaterialGpu.map_uv_row0).offset == 48;
$assert $reflect(BasicMaterialGpu.map_uv_row1).offset == 64;

struct GuiVertexGpu {
    float position_x;
    float position_y;
    float uv_x;
    float uv_y;
    uint  rgba;
}
$assert GuiVertexGpu::size == 20;
$assert $reflect(GuiVertexGpu.position_x).offset == 0;
$assert $reflect(GuiVertexGpu.position_y).offset == 4;
$assert $reflect(GuiVertexGpu.uv_x).offset == 8;
$assert $reflect(GuiVertexGpu.uv_y).offset == 12;
$assert $reflect(GuiVertexGpu.rgba).offset == 16;

struct GuiVertexRoot {
    GpuAddress vertices;
    Vec2f      scale;
    Vec2f      translate;
}
$assert GuiVertexRoot::size == 24;
$assert $reflect(GuiVertexRoot.vertices).offset == 0;
$assert $reflect(GuiVertexRoot.scale).offset == 8;
$assert $reflect(GuiVertexRoot.translate).offset == 16;

struct GuiFragmentRoot {
    TextureIndex source_texture;
    SamplerIndex source_sampler;
}
$assert GuiFragmentRoot::size == 8;
$assert $reflect(GuiFragmentRoot.source_texture).offset == 0;
$assert $reflect(GuiFragmentRoot.source_sampler).offset == 4;

struct CubePreviewRoot {
    TextureIndex source_texture;
    SamplerIndex source_sampler;
    float        lod;
    uint         face;
    uint         explicit_lod;
    uint         _pad0;
    uint         _pad1;
    uint         _pad2;
}
$assert CubePreviewRoot::size == 32;
$assert $reflect(CubePreviewRoot.source_texture).offset == 0;
$assert $reflect(CubePreviewRoot.source_sampler).offset == 4;
$assert $reflect(CubePreviewRoot.lod).offset == 8;
$assert $reflect(CubePreviewRoot.face).offset == 12;
$assert $reflect(CubePreviewRoot.explicit_lod).offset == 16;
$assert $reflect(CubePreviewRoot._pad0).offset == 20;
$assert $reflect(CubePreviewRoot._pad1).offset == 24;
$assert $reflect(CubePreviewRoot._pad2).offset == 28;

struct LightGpu {
    Vec4f position_range;
    Vec4f direction_cos_outer;
    Vec4f color_intensity;
    uint  kind;
    uint  layers;
    float cos_inner;
    uint  _pad0;
}
$assert LightGpu::size == 64;
$assert $reflect(LightGpu.position_range).offset == 0;
$assert $reflect(LightGpu.direction_cos_outer).offset == 16;
$assert $reflect(LightGpu.color_intensity).offset == 32;
$assert $reflect(LightGpu.kind).offset == 48;
$assert $reflect(LightGpu.layers).offset == 52;
$assert $reflect(LightGpu.cos_inner).offset == 56;
$assert $reflect(LightGpu._pad0).offset == 60;

struct StandardMaterialGpu {
    uint         kind;
    uint         flags;
    float        alpha_cutoff;
    float        metallic;
    Vec4f        base_color;
    Vec4f        emissive_strength;
    float        roughness;
    float        normal_scale;
    float        occlusion_strength;
    uint         _pad0;
    TextureIndex base_color_texture;
    SamplerIndex base_color_sampler;
    uint         base_color_uv_set;
    uint         base_color_present;
    Vec4f        base_color_uv_row0;
    Vec4f        base_color_uv_row1;
    TextureIndex metallic_roughness_texture;
    SamplerIndex metallic_roughness_sampler;
    uint         metallic_roughness_uv_set;
    uint         metallic_roughness_present;
    Vec4f        metallic_roughness_uv_row0;
    Vec4f        metallic_roughness_uv_row1;
    TextureIndex normal_texture;
    SamplerIndex normal_sampler;
    uint         normal_uv_set;
    uint         normal_present;
    Vec4f        normal_uv_row0;
    Vec4f        normal_uv_row1;
    TextureIndex occlusion_texture;
    SamplerIndex occlusion_sampler;
    uint         occlusion_uv_set;
    uint         occlusion_present;
    Vec4f        occlusion_uv_row0;
    Vec4f        occlusion_uv_row1;
    TextureIndex emissive_texture;
    SamplerIndex emissive_sampler;
    uint         emissive_uv_set;
    uint         emissive_present;
    Vec4f        emissive_uv_row0;
    Vec4f        emissive_uv_row1;
}
$assert StandardMaterialGpu::size == 304;
$assert $reflect(StandardMaterialGpu.kind).offset == 0;
$assert $reflect(StandardMaterialGpu.flags).offset == 4;
$assert $reflect(StandardMaterialGpu.alpha_cutoff).offset == 8;
$assert $reflect(StandardMaterialGpu.metallic).offset == 12;
$assert $reflect(StandardMaterialGpu.base_color).offset == 16;
$assert $reflect(StandardMaterialGpu.emissive_strength).offset == 32;
$assert $reflect(StandardMaterialGpu.roughness).offset == 48;
$assert $reflect(StandardMaterialGpu.normal_scale).offset == 52;
$assert $reflect(StandardMaterialGpu.occlusion_strength).offset == 56;
$assert $reflect(StandardMaterialGpu._pad0).offset == 60;
$assert $reflect(StandardMaterialGpu.base_color_texture).offset == 64;
$assert $reflect(StandardMaterialGpu.base_color_sampler).offset == 68;
$assert $reflect(StandardMaterialGpu.base_color_uv_set).offset == 72;
$assert $reflect(StandardMaterialGpu.base_color_present).offset == 76;
$assert $reflect(StandardMaterialGpu.base_color_uv_row0).offset == 80;
$assert $reflect(StandardMaterialGpu.base_color_uv_row1).offset == 96;
$assert $reflect(StandardMaterialGpu.metallic_roughness_texture).offset == 112;
$assert $reflect(StandardMaterialGpu.metallic_roughness_sampler).offset == 116;
$assert $reflect(StandardMaterialGpu.metallic_roughness_uv_set).offset == 120;
$assert $reflect(StandardMaterialGpu.metallic_roughness_present).offset == 124;
$assert $reflect(StandardMaterialGpu.metallic_roughness_uv_row0).offset == 128;
$assert $reflect(StandardMaterialGpu.metallic_roughness_uv_row1).offset == 144;
$assert $reflect(StandardMaterialGpu.normal_texture).offset == 160;
$assert $reflect(StandardMaterialGpu.normal_sampler).offset == 164;
$assert $reflect(StandardMaterialGpu.normal_uv_set).offset == 168;
$assert $reflect(StandardMaterialGpu.normal_present).offset == 172;
$assert $reflect(StandardMaterialGpu.normal_uv_row0).offset == 176;
$assert $reflect(StandardMaterialGpu.normal_uv_row1).offset == 192;
$assert $reflect(StandardMaterialGpu.occlusion_texture).offset == 208;
$assert $reflect(StandardMaterialGpu.occlusion_sampler).offset == 212;
$assert $reflect(StandardMaterialGpu.occlusion_uv_set).offset == 216;
$assert $reflect(StandardMaterialGpu.occlusion_present).offset == 220;
$assert $reflect(StandardMaterialGpu.occlusion_uv_row0).offset == 224;
$assert $reflect(StandardMaterialGpu.occlusion_uv_row1).offset == 240;
$assert $reflect(StandardMaterialGpu.emissive_texture).offset == 256;
$assert $reflect(StandardMaterialGpu.emissive_sampler).offset == 260;
$assert $reflect(StandardMaterialGpu.emissive_uv_set).offset == 264;
$assert $reflect(StandardMaterialGpu.emissive_present).offset == 268;
$assert $reflect(StandardMaterialGpu.emissive_uv_row0).offset == 272;
$assert $reflect(StandardMaterialGpu.emissive_uv_row1).offset == 288;
